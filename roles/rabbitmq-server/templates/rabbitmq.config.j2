[
  {rabbit,
    [
      {cluster_nodes, {[
{% for host in groups['openstack-controller'] %}
                      'rabbit@{{ hostvars[host]['ansible_hostname'] }}',
{% endfor %}
                      ''], disc}},
      {cluster_partition_handling, ignore}
    ]
  },
  {kernel,
    [
      {inet_dist_listen_min, 9000},
      {inet_dist_listen_max, 9005}
    ]
  }
].
